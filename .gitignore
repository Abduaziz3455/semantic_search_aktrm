# Model weights — multi-GB, provisioned locally rather than committed.
# Covers both the Python copy (models/bge-m3) and the Java ONNX export
# (java/models/bge-m3).
models/

# Java
java/target/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Qdrant local storage (uses a named docker volume anyway)
qdrant_storage/
